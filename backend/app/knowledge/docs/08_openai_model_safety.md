# 在线模型接入规则

## 结构化输出
在线 Agent 使用 Pydantic 输出结构对业务简报、SQL 草案、工具选择、实验方案和评审结果进行验证。模型输出不符合结构时最多重试三次，仍失败则明确标记 FAILED，不用离线固定答案伪装成功。

## 自带密钥
访客提供的 API Key 只保存在服务进程内存中，使用随机连接编号引用，三十分钟自动过期。密钥不写入 SQLite、日志、事件、导出文件或浏览器持久化存储。

## 最小披露
发送给模型的内容限于合成数据的字段说明、聚合质量指标、业务目标和知识片段。禁止发送真实姓名、手机号、邮箱、身份证、支付卡号、密码或密钥。
